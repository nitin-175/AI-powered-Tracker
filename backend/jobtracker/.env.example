# Local development example - DO NOT commit real keys
# Copy this file to .env (or set these in your environment) and restart the app
GEMINI_API_KEY=your_new_key_here

# Optional: adjust rate limit (per-minute)
GEMINI_MAX_REQUESTS_PER_MINUTE=60
